# Артефакты сборки, тестов, снимки, образ SD, кэши dotnet — всё в tmp/ (правило проекта)
/tmp/
# Оригинальные данные игр — не распространяются (в порт идут только сконвертированные
# из копии пользователя)
/Steam/
# Справочные материалы (чужие исходники, документация) — только для чтения, не в репозитории
/REF/
# Эмулятор Unreal — копия со своим git
/unreal/
# .NET
bin/
obj/
*.user
# Редакторы и ОС
/.vscode/
.vs/
*.swp
*~
Thumbs.db
desktop.ini
# Логи посторонних программ, попавшие в корень
debug.log
# Старые версии файлов, переписанных на ассемблер
*.old
